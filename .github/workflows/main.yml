name: Trigger Workflow

on:
  push:
    branches:
      - votha

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: add cert
        shell: bash
        run: ls

      # Add your desired actions here
